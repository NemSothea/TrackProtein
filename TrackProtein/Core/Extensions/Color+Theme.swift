import SwiftUI

extension Color {
    /// Brand accent — energetic orange.
    static let proteinOrange = Color(red: 1.0, green: 0.42, blue: 0.21)
    static let proteinDeep = Color(red: 0.85, green: 0.25, blue: 0.10)
}
